// Solid placeholder used wherever a real poster/thumbnail is not available.
const PLACEHOLDER = '#171717'

/** A solid fallback color. */
export function colorFor(_id: string): string {
  return PLACEHOLDER
}
